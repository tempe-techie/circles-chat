# Local API server only (npm run dev:api). Vite proxies /api to port 8080.
MYLOCALHOST=1

# HMAC secret for signed reaction payment intents (optional locally; uses dev default)
# REACTION_HMAC_SECRET=change-me-in-production
